import React, {useState} from "react";

const TabRow = (props) => {
    const onClickHandler = (e) => {
        e.preventDefault();
        // some code
    };

    return (
        <>
        <div>
            
        </div>
        </>
    )
}